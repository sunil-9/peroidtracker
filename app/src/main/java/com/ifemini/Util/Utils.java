package com.ifemini.Util;

import android.app.Application;

public class Utils extends Application {
    @Override
    public void onCreate() {
        super.onCreate();
//        Realm.init(this);
//        RealmConfiguration con = new RealmConfiguration.Builder().name("peopledata.realm").build();
//        Realm.setDefaultConfiguration(con);
    }
}
